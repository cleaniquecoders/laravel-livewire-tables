{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite build --watch",
        "build": "vite build",
        "build:assets": "node build-assets.mjs"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0",
        "concurrently": "^9.0",
        "esbuild": "^0.24",
        "laravel-vite-plugin": "^2.0",
        "tailwindcss": "^4.0",
        "vite": "^7.0"
    }
}
