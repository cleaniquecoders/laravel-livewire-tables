// Workbench JS entry. Livewire 4 bundles Alpine, and Flux ships its own
// scripts via @fluxScripts, so this entry is intentionally minimal — it
// exists so Vite has a JS input alongside the Tailwind CSS build.
